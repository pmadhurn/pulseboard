{
  "name": "pulseboard",
  "version": "2.1.0",
  "description": "PulseBoard — zero-idle, on-demand server health dashboard (CPU, memory, swap, storage, network, disk I/O, processes).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
